{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "mix_task",
      "name": "Phoenix Server",
      "request": "launch",
      "task": "phx.server",
      "projectDir": "${workspaceFolder}",
      "env": {
        "MIX_ENV": "dev"
      }
    }
  ]
}
